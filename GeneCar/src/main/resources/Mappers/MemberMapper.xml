<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
        
<mapper namespace="genecar.member.mapper.MemberMapper">

	<select id="selectLogin" parameterType="memberVO">
		SELECT * FROM MEMBER
	</select>



</mapper>